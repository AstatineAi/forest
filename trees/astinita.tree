\title{Hengyu Ai}
\taxon{person}
\meta{institution}{[ShanghaiTech University](https://www.shanghaitech.edu.cn/eng/)}

\import{base-macros}

\p{
  Undergraduate @ ShanghaiTech University
}

\p{
  如果这行中文 & 下方的公式显示正常 & 代码有高亮，说明网页加载正常。
}

##{
  T = \lambda f. (\lambda x. f (x x)) (\lambda x. f (x x))
}
\codehsk
{primes = filterPrime [2..] where
  filterPrime (p:xs) =
    p : filterPrime [x | x <- xs, x `mod` p /= 0]
}